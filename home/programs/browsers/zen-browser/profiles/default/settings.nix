{
  browser = {
    startup.homepage = "https://schwem.io";
  };

  layout = {
    css.prefers-color-scheme.content-override = 3;
  };

  zen = {
    theme = {
      use-system-colors = true;
    };
  };
}
