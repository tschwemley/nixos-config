# Hosts
Contains configuration for all hosts managed by nix. Any options that are host-specific are
defined here. Currently those are:
- hardware configuration
- networking rules

<!-- # Initialization Steps -->
<!-- ## Install Nix -->
<!-- sh <(curl -L https://nixos.org/nix/install) --[no-|]daemon # no-daemon for single; daemon for multi -->
<!---->
<!-- ## Generate configuration -->
<!-- nixos-generate-config -->
